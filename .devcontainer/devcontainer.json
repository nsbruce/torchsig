{
    "name": "${localWorkspaceFolderBasename}",
    "image": "pytorch/pytorch:2.0.1-cuda11.7-cudnn8-runtime",
    "runArgs": ["--gpus", "all"],
    "remoteUser": "root",
    "mounts" :[
        "source=/rfi-data/,target=/data,type=bind,consistency=cached"
    ],
    "onCreateCommand": "apt-get update && apt-get install -y build-essential libgl1-mesa-glx",
    "postCreateCommand": "pip3 install -e ${containerWorkspaceFolder} && pip3 install notebook jupyterlab",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter"
            ]
        }
    }
}